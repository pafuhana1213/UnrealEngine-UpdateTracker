# For GitHub API
PyGithub

# For Google Gemini API (unified Google GenAI SDK; replaces the deprecated google-generativeai)
google-genai

# For local development (reading .env files)
python-dotenv

# General purpose HTTP client (fallback)
requests